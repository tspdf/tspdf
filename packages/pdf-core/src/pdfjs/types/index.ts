/**
 * PDF.js types - only the ones we actually use in our project
 */
export type { PDFDocumentProxy } from './PDFDocumentProxy';
export type {
  PageViewport,
  PDFPageProxy,
  RenderParameters,
  RenderTask,
} from './PDFPageProxy';
export type { TextLayer, TextLayerParameters } from './TextLayer';
